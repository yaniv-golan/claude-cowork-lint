## Summary

<!-- One paragraph: what does this PR change and why? -->

## Spec mapping

<!-- Which section of `docs/internal/SPEC.md` does this address? Or: why doesn't this map to one (e.g. internal refactor, docs)? -->

## Test plan

- [ ] Added a failing test, then made it pass
- [ ] `npm test` passes locally
- [ ] `npm run check` (typecheck + lint + tests) is clean
- [ ] If user-visible behavior changed, added a `CHANGELOG.md` entry

## Notes for reviewers

<!-- Anything tricky? Decisions worth flagging? -->
